language: php
php:
  - 5.3
mysql:
  database: mydb
  username: mydb
  encoding: utf8

before_script: 
  - cd bdd
  - sudo apt-get install drush
  - drush make event_calendar.make drupal

script: php behat.phar 
