# behat.yml
default:
  # ...
  extensions:
    mink_extension.phar:
      mink_loader: 'mink.phar'
      base_url:    'http://events.sandbox.opensourcery.com'
      goutte:      ~
      selenium2:   ~
      show_cmd: 'firefox %s'
